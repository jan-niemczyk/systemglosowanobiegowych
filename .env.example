# --- baza danych ---
POSTGRES_DB=esog
POSTGRES_USER=esog
POSTGRES_PASSWORD=zmien_to_haslo_w_prod
DATABASE_URL=postgresql://esog:zmien_to_haslo_w_prod@localhost:5432/esog

# --- Auth.js ---
# wygeneruj: openssl rand -base64 32
NEXTAUTH_SECRET=zmien_to_w_produkcji_dlugi_losowy_ciag_minimum_32_znaki
NEXTAUTH_URL=http://localhost:3000

# --- pierwsze uruchomienie ---
# ustaw na true tylko przy pierwszym deploju, żeby uruchomić seed z kontami testowymi
INIT_SEED=false
